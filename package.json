{
  "name": "node-http-server",
  "version": "1.0.0",
  "description": "Node.js example server starter project",
  "scripts": {
    "start": "yarn workspace functions start && firebase emulators:start --only functions  --project florida-fish",
    "start-client": "yarn workspace client start",
    "startold": "tsc --build && yarn workspace client start"
  },
  "private": true,
  "workspaces": ["packages/*", "functions"],
  "dependencies": {},
  "devDependencies": {
    "firebase-tools": "8.0.3",
    "typescript": "3.8.3",
    "firebase-admin": "8.10.0"
  },
  "keywords": []
}
